/**
 * This module was removed — the active Demucs integration lives in tools/vocals.ts
 * which uses a cross-platform Node.js spawn chain for fire-and-forget background processing.
 *
 * This placeholder exists to prevent import errors if anything still references this path.
 * It can be safely deleted once all imports are verified as removed.
 */

export {};
